"""CLI entry point, argument definitions, and command handlers.

The entry point is :mod:`kernel_atlas.commands.cli`. Handlers receive shared
CLI services explicitly so imports flow from the entry point to handlers
and then to domain modules.
"""
