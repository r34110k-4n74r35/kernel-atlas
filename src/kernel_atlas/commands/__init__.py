"""Internal CLI argument definitions and command handlers.

The supported entry point remains :mod:`kernel_atlas.cli`. Handlers receive
shared CLI services explicitly so imports flow from the entry point to this
package and then to domain modules.
"""
